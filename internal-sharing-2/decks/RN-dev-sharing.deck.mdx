---
title: Sharing
date: 2020-08-19
---

import DeckTitle from "gatsby-theme-slidedeck/src/components/deckTitle"

<DeckTitle
  title="Github Profile Readme | Google SPS 2020"
  subtitle="Eugene Teu"
/>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1>Before we continue....</h1>
  <h2>Ending my internship this fri</h2>
  <h3>Thanks for all the guidance + lunch sessions</h3>

  <p>Hope to come back!</p>
  
</div>


gatsby-slide

<h2 style={{ color: "tomato", textAlign: "center" }}>Github Pages!</h2>

Everyone's favourite way of pimping up our portfolio

Pros:

- Easy to deploy
- Great for static content

Cons:

- Manually maintaining and Updating it
- Even if we automate the development: still redirected away from Github

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
  }}
>
  <h2> What if there was a better way of #learningInPublic ? </h2>
  <h3> Don't require an entire site as dependency? </h3>
</div>

gatsby-slide

<h2 style={{ color: "tomato", textAlign: "center" }}>
  Introducing: Github Profile Readme!
</h2>

![Example](/images/github-readme-example.png)

gatsby-slide

<h2 style={{ color: "tomato", textAlign: "center" }}>
  Introducing: Github Profile Readme!
</h2>

![Example](/images/github-readme-example-2.png)

gatsby-slide

<h2 style={{ color: "tomato", textAlign: "center" }}>Why Profile Readme?</h2>

Pros:

- VERY easy to deploy
- Does not draw attention away from your repositories
- Viewers stay on same page

Cons:

- Is there really any?

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ color: "green", textAlign: "center" }}>Demo Time</h1>
</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
  }}
>
  <h1>Google SPS</h1>
</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
<h2 style={{ color: "tomato", textAlign: "center" }}>
  Google Software Product Sprint APAC
</h2>

<h3>
  Formerly known as Google <span style={{ color: "tomato" }}>CodeU</span>
</h3>

- Similar Programs across all regions (Greater china, india etc)
- 11 Week program
- " Software Product Sprint students design and build web apps, gain applied Data Structures and Algorithm skills, and receive tailored career development resources."
- Mentorship by a <span style={{ color: "tomato" }}>Google Engineer</span>
- Total of 43 students for this cohort in SG

</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
<h1 style={{ color: "tomato", textAlign: "center" }}>Cool freebies!</h1>

### Swag

![Freebies](/images/swag-1.png)

</div>

gatsby-slide

<div style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}>
  <h1 style={{ color: "tomato", textAlign: "center" }}>Cool freebies!</h1>

### Cupcakes

![Freebies](/images/cupcakes.png)

</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ textAlign: "center" }}>Curriculum</h1>
  <h2>11-week program split into two portions:</h2>
  <ul>
    <li>
      First 4 weeks:{" "}
      <span style={{ color: "purple" }}>Intro to Web Development</span>
    </li>
    <li>
      Next 7 weeks: <span style={{ color: "red" }}>Build a product</span>
    </li>
  </ul>
</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ textAlign: "center" }}>Intro to Web Development</h1>
  <h2>Back 2 basics</h2>
  <ul>
    <li>Create portfolio site with vanilla CSS and Js</li>
    <li>
      Implement simple calls to Blobstore API with using vanilla js and Java
      Servlets
    </li>
  </ul>
</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ textAlign: "center" }}>My Site</h1>

![portfolio](/images/sps-portfolio.png)

[Link](https://eugene-sps.uc.r.appspot.com/)

</div>

gatsby-slide

<h1 style={{ textAlign: "center" }}>Some Code Snippets</h1>

```js live=true
/** Fetch image upload url and show form to submit comment with image */
async function fetchBlobstoreUrlAndShowForm() {
  const response = await fetch("/blobstore-upload-url")
  const imageUploadUrl = await response.text()
  const messageForm = document.getElementById("my-form")
  messageForm.action = imageUploadUrl
  messageForm.classList.remove("blob-store")
}
```

gatsby-slide

<h1 style={{ textAlign: "center" }}>Some Code Snippets</h1>

```java live=true
/** Servlet responsible for return url to servlet for user to upload. */
@WebServlet("/blobstore-upload-url")
public class BlobstoreUploadUrlServlet extends HttpServlet {
  private final  BlobstoreService blobstoreService =
      BlobstoreServiceFactory.getBlobstoreService();

  @Override
  public void doGet(HttpServletRequest request, HttpServletResponse response)
  throws IOException {
    String uploadUrl = blobstoreService.createUploadUrl("/comments");

    response.setContentType("text/html");
    response.getWriter().println(uploadUrl);
  }
```

gatsby-slide

<h1 style={{ textAlign: "center" }}>Other Notable things</h1>

### Stuff I did

- Code reviews
- Applied algo + ds in week 4 (Leetcode question but in the context of a real application - Merging Intervals)
- Weekly Workshops by Google
  - Career development
  - Past internship Sharing
  - Technical interview workshops

### Mock Interviews

- Mock Interviews 1 & 2
- 2 session with Google engineers
- Conducted over google docs

gatsby-slide

<h1 style={{ textAlign: "center" }}>Software Sprint</h1>

<h2 style={{ textAlign: "center" }}> The Fun Portion</h2>

<h2 style={{ textAlign: "center" }}> GroupBuy20</h2>

### What it is

- Our attempt at solving the last mile delivery problem
- A platform to enable people to post requests and ask for things
- Users are able to complete requests to earn a fee
- Think of Grabfood but without grabbing your wallets!
- Leverage on the community

### Use case

- Within University Dorms
- Within an organisation
- Users can help each other buy and deliver stuff (not limited to food)

### Who

- 3 participants including me (all year 2 )

[Link](https://spsteam10-748f8.firebaseapp.com/)

gatsby-slide

import { FaReact, FaFireAlt } from "react-icons/fa"

<h1 style={{ textAlign: "center" }}>Tech Stack</h1>

<div
  style={{
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ marginRight: "12px", fontSize: "30vh" }}>
    <FaReact />
  </h1>
  <h1 style={{ marginLeft: "12px", fontSize: "30vh" }}>
    <FaFireAlt />
  </h1>
</div>

gatsby-slide

<h1 style={{ color: "blue", textAlign: "center" }}>
  #Firebase makes things easy
</h1>
<h2>Google Login</h2>

```js live=true
  const uiConfig = {
        signInFlow: "popup",
        signInSuccessUrl: "/dashboard",
        signInOptions: [
            firebase.auth.GoogleAuthProvider.PROVIDER_ID,
            firebase.auth.EmailAuthProvider.PROVIDER_ID,
        ],
    }

    <StyledFirebaseAuth
                uiConfig={uiConfig}
                firebaseAuth={firebase.auth()}
          />
```

gatsby-slide

<h1 style={{ color: "blue", textAlign: "center" }}>#Firebase + Context</h1>

```js live=true
const config = {
  apiKey: process.env.REACT_APP_API_KEY,
  .......
}
const firebaseApp = firebase.initializeApp(config)
export const db = firebase.firestore(firebaseApp)
ReactDOM.render(
  <FirebaseContext.Provider value={{ firebase: firebase, firebaseApp }}>
    <App />
  </FirebaseContext.Provider>,
  document.getElementById("root")
)
```

gatsby-slide

<h1 style={{ color: "blue", textAlign: "center" }}>
  API approach (Technically there isn't one)
</h1>

```js live=true
const getOpenOffers: () => Promise<Offer[]> = async () => {
  const offersRef = await db
    .collection(COLLECTION_OFFERS)
    .where("status", "==", Status.OPEN)
    .get()
  const temp: firebase.firestore.QueryDocumentSnapshot<firebase.firestore.DocumentData>[] = []
  offersRef.forEach(offerSnapshot => temp.push(offerSnapshot))
  const offers: Offer[] = []
  await Promise.all(
    temp.map(taskSnapShot => offerConverter.fromFirestore(taskSnapShot))).then(values => {
      values.forEach(value => offers.push(value))
    }).catch(e =>
      console.error(`Encountered error while retrieving offer: ${e.message}`))
  return offers
    .filter((value: Offer) => value.status === Status.OPEN)
    .sort((prev, curr) =>
      sortByReverseOrder(prev.expectedDeliveryTime, curr.expectedDeliveryTime))
}
```

gatsby-slide

<h2 style={{ color: "tomato", textAlign: "center" }}>
  What I have learnt
</h2>

<h3 style={{textAlign: 'left'}}>
  Not everyone is into <span style={{ color: "tomato" }}> #webDevelopment</span>
</h3>

- Spending time around people of similar interests 
- Assume that things are trivial and easy 
- Always think about learning curve when we introduce technology 
- Accept that everyone has different learning abilities -> be patient

<h3 style={{textAlign: 'left'}}>
 It was fun to be a <span style={{ color: "tomato" }}> #cowboyDeveloper</span>
</h3>

- Fast bootstrapping 
- When speed is of the essence, the way we write software is slightly different
- Love the challenge and the pressure 
- Time and space for everything 

<h3 style={{textAlign: 'left'}}>
 So many new tech out there to <span style={{ color: "tomato" }}> #explore</span>
</h3>

- Field moves so fast 
- We have to keep exploring to stay relevant
- Literally forced to reinvent ourselves



<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
  }}
>
  <h1>The Most Interesting Thing out of all this.....</h1>
</div>

gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center",
  }}
>
  <h1>10x thinking</h1>
</div>

gatsby-slide


![10x](/images/10x.jpg)

gatsby-slide

### What it is:

- ~~Google brainwashing~~
- Google's culture of innovation
- To put the idea simply: true innovation happens when you try to improve something by 10 times rather than by 10%.
- Pushes you beyond existing models and forces you to totally reimagine how to approach a problem.
- "10x is easier than 10%"

### Where can you find it:

- Google x - the moonshot company
- Projects like Tidal, Everyday Robot etc

gatsby-slide


<h1 style={{ textAlign: "center" }}>Everyday Robot</h1>

![everyday-robot](/images/everyday-robots.png)

- Teaching robots how to operate in unstructured environment

gatsby-slide

<h1 style={{ textAlign: "center" }}>I would like to leave everyone with a question today</h1>


<h2  style={{ color:'tomato', textAlign: "center" }}>How can we adopt this in our daily lives? </h2>


gatsby-slide

<div
  style={{
    display: "flex",
    height: "100%",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <h1 style={{ color: "tomato", textAlign: "center" }}>The End!</h1>
</div>

gatsby-slide
